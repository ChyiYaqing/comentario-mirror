# Almost empty secrets file for running the app locally and e2e tests
postgres:
  host:     localhost # 'comentario-postgres-postgresql' when running in the cluster
  port:     5432 # Port is optional, defaults to 5432
  database: comentario
  username: postgres
  password: postgres

smtpServer:
  host:
  port:
  username:
  password:

idp:
  github:
    key: blah
    secret: bluh
  gitlab:
    key: blah
    secret: bluh
  google:
    key: blah
    secret: bluh

akismet:
  key:
